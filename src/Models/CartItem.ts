export type CartItem = {
  productId: string;
  productName: string;
  price: number;
  quantity: number;
  image?: string;
};

export type CartItem_ = {
  ProductId: string;
  productName: string;
  price: number;
  Quantity: number;
};